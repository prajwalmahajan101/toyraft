# Phase M13 — iptables / netns partition chaos (Linux-only)

**Branch:** `feature/netns-chaos`
**Base SHA:** `f99b955` (`main` at branch-off)
**Merged SHA:** (filled at squash-merge)
**Dates:** 2026-07-04 → 2026-07-04

## Goals

Give ToyRaft a **real-network** partition test — one that exercises the failure
mode the Phase-11 loopback chaos cannot reach — behind a build tag, using Linux
network namespaces for automatic teardown:

> Add real-network partition tests behind a build tag, using Linux netns for
> automatic teardown; partition two nodes with `iptables -j DROP`, assert
> recovery, and prove the namespace is gone after `t.Cleanup`. Retire risk D-1
> (loopback masking that inproc and process-kill cannot detect).

## What shipped

All new code lands under `test/chaos/netns/`, plus one CI job, one ADR, one
ADR amendment, a branch-protection update, and this journal. Landed across
commits on `feature/netns-chaos`:

- **13-01 — netns chaos harness** (`a2fe4b9`, `139d53a`).
  `test/chaos/netns/harness.go` (473L, `//go:build linux && netns`, package
  `netns`): a root-namespace bridge + **N=3** namespaces + 3 veth pairs, each
  node its own `10.<octet>.0.1{0,1,2}/24` real IP (a genuine L3 hop), names
  randomized per-run from the pid. Each `toyraftd` is spawned INTO its namespace
  via `sudo ip netns exec <ns> <bin> …` with `SysProcAttr{Setpgid: true}`
  (ADR-0018 PGID discipline). `partition(a,b)`/`heal(a,b)` install/remove
  per-namespace `iptables -A/-D INPUT/OUTPUT -s/-d <peerip> -j DROP` inside the
  two victim namespaces (never a rule in the root/bridge ns). Teardown is a
  single LIFO `t.Cleanup`: kill PGIDs → `cmd.Wait` → `ip netns del` each ns →
  `ip link del` the bridge → `assertNoLeak` (no `tr-<pid>-nX` in `ip netns
  list`). Owns the full `/status` oracle API (`getStatus`/`roleOf`/
  `commitIndexOf`/`findLeader`/`maxCommitIndex`/`majorityCommitAtLeast`/
  `setKV`/`getKV`) so 13-02 compiles against this one file. The **Phase-12
  history-capture seam is dropped** — no recorder, no `clock`/`raftest` import
  (SC1 needs no history artifact). Zero new Go deps.
- **13-02 — `TestNetnsPartition` (SC1/SC2)** (`4bb2975`).
  `test/chaos/netns/netns_test.go` (153L, `//go:build linux && netns`):
  boot 3 namespaced nodes → wait for a leader → pre-partition set/get →
  baseline `commit_index` → `h.partition(0,1)` (cut the single n0↔n1 link so n2
  bridges a surviving majority) → **positive commit oracle** (`h.majorityCommit
  AtLeast(baseline+M, majority=2, deadline)` over the `/status` commit_index
  seam — durable replication on the surviving side, ADR-0018, never "no panic")
  → `h.heal(0,1)` → full re-convergence. SC2 is a three-layer skip guard as the
  first statements: the build tag excludes non-linux at compile;
  `runtime.GOOS != "linux"`, `os.Geteuid() != 0 && !sudoAvailable()` (where
  `sudoAvailable()` runs `sudo -n true`), and `testing.Short()`. Deadlines
  widened to 8s from process-kill's 1s/2s for the real L3 DROP path.
- **13-03 — phase close** (this plan: `d852728`, `8c76567`, plus the docs commit
  that lands this file).
  `.github/workflows/ci.yml`: a dedicated top-level **`netns`** job on
  `ubuntu-latest` — a capability probe (`sudo iptables --version; sudo ip netns
  add/del probe`) then the terminal `sudo -E env "PATH=$PATH" go test
  -tags=netns -timeout 10m ./test/chaos/netns`, no race detector, no
  conditional guard / failure-tolerating modifier / re-run loop. The four
  existing jobs (lint/test/commitlint/build) are unchanged. `docs/adr/0020`;
  the ADR-0002 required-check-list amendment (8th check `netns`); and the
  branch-protection update requiring `netns` (below).

## ADRs introduced

- **ADR-0020 — netns/iptables partition chaos and a dedicated CI job**
  (Accepted). Ratifies: (1) subprocess `ip netns exec` over in-process `setns`
  (the goroutine↔thread↔namespace footgun); (2) the N=3 separate-namespace +
  veth + bridge topology partitioned by per-ns `iptables -j DROP`; (3) `ip netns
  del` auto-teardown + the namespace-absence leak gate (SC1); (4) the `linux &&
  netns` build tag + `runtime.GOOS`/sudo skip (SC2); (5) the **dedicated `netns`
  CI job** (Option A) and its ADR-0002 required-check consequence + the
  SC3-wording reinterpretation.
- **ADR-0002 amendment.** The **Required-check names** list gains an 8th
  backticked `netns` bullet plus a dated "Amended 2026-07-04 (Phase 13 /
  ADR-0020)" note pointing to this journal for the branch-protection replay.

## Decisions made (non-ADR)

- **N=3** — the minimum clean odd majority for "partition two, third holds
  quorum" (RESEARCH Open-Q4).
- **No race detector on the `netns` job** — the test orchestrates external
  daemons; `-race` only instruments the barely-concurrent test process, adding
  cost with no signal (RESEARCH Open-Q3).
- **Deadlines widened to 8s** from process-kill's 1s/2s — a real L3 `DROP`
  blocks TCP until the ~150ms `SendTimeout` then retries, so recovery over a
  veth hop is slower than loopback (RESEARCH Pitfall 7).
- **Per-run randomized ns/bridge/subnet names** from the pid, for idempotency
  against a crashed prior run (RESEARCH Pitfall 4); assumes one netns test per
  `go test` process.
- **Recorder dropped** — no Phase-12 history for this path (SC1 is
  partition + recovery + namespace-absent teardown, not a Porcupine artifact).
- **SC3 read as "dedicated job = the linux-amd64 entry."** RESEARCH flagged the
  tension between a dedicated `ubuntu-latest` job (Option A) and the literal
  "matrix entry" wording; resolved to Option A (one linux-amd64 runner, never
  macOS) and recorded in the ci.yml comment, ADR-0020, and here.

## Branch-protection replay (ADR-0002 `.journal` capture rule)

The dedicated `netns` job adds the 8th required check, so branch protection on
`main` was updated via `gh api` — the **narrow `required_status_checks`
endpoint** (touches only the contexts array), not a full replace-all
`PUT .../protection`. `gh auth status` confirmed the active token
(`prajwalmahajan101`) carries the `repo` scope, sufficient for branch-protection
admin on an owned repo; the PATCH succeeded.

Exact command (contexts derived from the live BEFORE snapshot to avoid drift):

```bash
OWNER_REPO=$(gh repo view --json nameWithOwner -q .nameWithOwner)   # prajwalmahajan101/toyraft
gh api "repos/$OWNER_REPO/branches/main/protection/required_status_checks" > /tmp/rsc-before.json
jq '{strict: .strict, contexts: (.contexts + ["netns"] | unique)}' /tmp/rsc-before.json > /tmp/rsc-new.json
gh api -X PATCH "repos/$OWNER_REPO/branches/main/protection/required_status_checks" --input /tmp/rsc-new.json
```

**Contexts BEFORE (7):**

```
build (cross-compile)
commitlint
lint
test (macos-latest / go 1.25.x)
test (macos-latest / go 1.26.x)
test (ubuntu-latest / go 1.25.x)
test (ubuntu-latest / go 1.26.x)
```

**Contexts AFTER (8):** the same seven **plus** `netns`. Verified non-regression
by diff, not merely by asserting `netns` appeared:

- `comm -23 /tmp/ctx-before.txt /tmp/ctx-after.txt` → **empty** (no context dropped)
- `grep -q '^netns$' /tmp/ctx-after.txt` → present (netns added)
- `strict` preserved: `true` (before) = `true` (after)

**Other protection toggles — all SAME before/after** (spot-checked per
ADR-0002; `jq -c` equality on each):

| Toggle | Value (before == after) |
| ------ | ----------------------- |
| `required_pull_request_reviews.required_approving_review_count` | `0` (solo-maintainer self-merge, ADR-0002) |
| `required_linear_history.enabled` | `true` |
| `required_conversation_resolution.enabled` | `true` |
| `restrictions` | not set (`null`) — unchanged |
| `enforce_admins.enabled` | `true` |

Outcome: **SC3 enforced end-to-end.** `main` now requires all eight checks; the
existing seven contexts and every other protection toggle survived the update.
(`gh api` accepts an arbitrary context string, so `netns` is required BEFORE its
first observed run; the first CI run on the phase PR then satisfies it.)

## Risks retired

- **D-1 (loopback masking).** Retired. Phase-11's inproc + process-kill chaos
  run over `127.0.0.1`, where the kernel loopback path cannot exhibit a real
  partition — a `DROP` there is not an unreachable peer. Phase 13 gives each
  `toyraftd` its own network namespace + veth + non-loopback IP on a shared
  bridge and severs a single link with per-namespace `iptables -j DROP`, so a
  real L3 `DROP` across distinct addresses now exercises the
  dial/timeout/reconnect path that loopback cannot. `TestNetnsPartition` asserts
  surviving-majority commit through the partition and full re-convergence after
  heal (positive oracle over the `/status` commit_index seam).

## SC evidence

| SC | Property | Evidence (exact command) |
| -- | -------- | ------------------------ |
| SC1 | netns partition + assert recovery + namespace gone after `t.Cleanup` | On a root / passwordless-sudo Linux host: `sudo -E env "PATH=$PATH" go test -tags=netns -timeout 10m ./test/chaos/netns` runs `TestNetnsPartition` (partition n0↔n1, surviving-majority commit oracle, heal, re-converge); `ip netns list` shows no `tr-<pid>-nX` namespace afterward (the harness `assertNoLeak` gate). |
| SC2 | skipped (not failed) on non-Linux / non-root | `go test -tags=netns -short ./test/chaos/netns` → `ok` (SKIP, not FAIL) via `runtime.GOOS`/`Geteuid`/`sudo -n`/`testing.Short`; the `//go:build linux && netns` tag makes macOS report the package excluded even with `-tags=netns`. On this non-root box the run SKIPS: "netns chaos needs root or passwordless sudo". |
| SC3 | CI runs the netns job on the linux-amd64 entry only | The dedicated `netns` job in `.github/workflows/ci.yml` (ubuntu-latest, capability probe + `sudo -E env "PATH=$PATH" go test -tags=netns …`, no race detector, no conditional guard / failure-tolerating modifier / re-run loop); enforced by the non-regressing branch-protection PATCH requiring `netns` (contexts 7 → 8, no drop). `awk '/^  netns:/,0' .github/workflows/ci.yml \| grep -Eq 'continue-on-error\|if:\|retry\|-race'` returns non-zero (none present). |

## PROC-03 (RFC) — evaluated, NOT triggered

PROC-03 was evaluated and does **not** apply: the netns chaos is internal test
infrastructure under `test/chaos/netns/`. It touches no public API and no
Phase-1 frozen contract — `pkg/raft`, `pkg/storage`, `pkg/transport/*`, and
`cmd/toyraftd` are unchanged (the daemon binds arbitrary namespace IPs from its
existing `-peers`/`-listen` flags with no code change). No RFC is owed.

## What surprised us

- **Option A's honest cost was a branch-protection change, not a test change.**
  Reusing the `test` job (as Phase 11/12 did) is free; a dedicated `netns` job
  is the cleaner expression of "linux-amd64 entry only" but adds the 8th
  required check — which ADR-0002 explicitly says obliges an ADR entry *and* a
  branch-protection update. The narrow `required_status_checks` endpoint made
  the update safe (it touches only the contexts array), and a before/after diff
  proved the other seven contexts and four toggles survived.
- **`gh api` will require a context that has never been observed.** `netns` was
  added to the required set before its first CI run; the first phase-PR run then
  satisfies it. No chicken-and-egg wait.
- **The verify greps false-positive on prose describing an absence.** The
  terminal-failure comment and the "no race detector" comment originally
  contained the literal `if:` / `continue-on-error` / `-race` tokens the SC3
  gate greps for — the same class of reword the 13-01/13-02 executors hit. The
  step itself is a plain `run:` with zero modifiers; only the comment prose was
  reworded to carry none of the forbidden tokens.

## Follow-ups

- [ ] **Phase 14 — observability, ADR backfill, README, GoReleaser release** —
  the last phase before v1.0.0.
- [ ] A future ADR could parameterize N (fixed at 3) for a larger partition
  topology.

## Seeds for next phase

- The netns harness (`test/chaos/netns/harness.go`) is the reusable scaffold for
  any real-L3 chaos: bridge + N namespaces + veth, spawn-into-ns via `ip netns
  exec`, `iptables` partition, and `ip netns del` teardown + absence gate. It is
  linux-amd64-only and needs root / passwordless sudo.
- The `netns` CI job is terminal and now a required check on `main`; if a future
  phase renames it, update ADR-0002's required-check list and re-run the
  branch-protection `gh api` PATCH in the same PR (this journal is the replay).
- The recorder is dropped for this path by design — do not expect a Phase-12
  `history.json` artifact from `test/chaos/netns`.
